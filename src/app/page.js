import Stacksync_flow from './stacksync-flow/page';

export default function Home() {
  return (
    <main>
      <Stacksync_flow></Stacksync_flow>
    </main>
  );
}

// save workflow as a json into a file. deploy to glocud send link (flow config) and github 
